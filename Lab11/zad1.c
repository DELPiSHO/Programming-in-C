#include <stdio.h>
int main()
{
	int a;
	double b;
	char c;
	int tab[100];
	printf(" int = %i, char = %i, double = %i, tablica = %i.\n", (int)sizeof(a),(int)sizeof(c),(int)sizeof(b),(int)sizeof(tab));
	return 0;
}
